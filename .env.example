# Copy this file to `.env.local` (Next.js reads that automatically).
# `.env.local` is gitignored; this example is committed.

# Base URL of the StudyCompass backend API.
# NEXT_PUBLIC_ prefix is required so the value is available in the browser.
NEXT_PUBLIC_API_URL=http://localhost:4000
